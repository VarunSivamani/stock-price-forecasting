    # st.table(final_graph[len(final_graph)-n_days:])

    ranger = []

    for i in range(0,n_days):
        ranger.append(i)
    
    price = final_graph[len(final_graph)-n_days:]

    unpacked_list = [item for sublist in price for item in sublist]

    df = pd.DataFrame({"Date":ranger, "Price":unpacked_list})

    st.table(df)

    fig = go.Figure()
    fig.add_trace(go.Scatter(x=df["Date"], y=df["Price"], name="forecast"))
    # # # # fig.add_trace(go.Scatter(x=data['Date'], y=data['Close'], name="stock_close"))
    fig.layout.update(title_text='Time Series data with Rangeslider', xaxis_rangeslider_visible=True)
    st.plotly_chart(fig)


    # st.write(len(ds_new))

    unpacked_list = [item for sublist in final_graph for item in sublist]


    x_date = []

    for i in range(len(ds_new)):
        x_date.append(i)

    df = pd.DataFrame({"Date":x_date, "Price":unpacked_list})

    # st.table(df)

    fig = go.Figure()
    fig.add_trace(go.Scatter(x=df["Date"], y=df["Price"], name="forecast", marker=dict(color='blue')))
    # # # # fig.add_trace(go.Scatter(x=data['Date'], y=data['Close'], name="stock_close"))
    fig.layout.update(title_text='Time Series data with Rangeslider', xaxis_rangeslider_visible=True)
    st.plotly_chart(fig)