# legacy — superseded variants kept for traceability (not used by app.py)

* `app_predict_only.py` — early predict-only Streamlit app (was repo-root `app.py`,
  originally `Testing deployment full stack/app.py`).
* `Final/` training-era demos remain in `notebooks/Final/`.

Use `../app.py` (loads weights from `../models/`) as the canonical entrypoint.
